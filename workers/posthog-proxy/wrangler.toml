# PostHog Reverse Proxy Worker
# Deploy: cd workers/posthog-proxy && npx wrangler deploy
# Then add custom domain e.highcontrasttattoo.com in Cloudflare dashboard

name = "posthog-proxy"
main = "index.js"
compatibility_date = "2024-01-01"

# Add custom domain in Cloudflare Dashboard:
# Workers & Pages → posthog-proxy → Settings → Domains & Routes
# Add: e.highcontrasttattoo.com
#
# DNS: Cloudflare will auto-create the record when you add the domain
